# require

**项目必要注释（整体要求）**
审查docs文件夹下的所有文件内容，严格按照此来编写。使用 Next.js 最佳实践！不准出现中文乱码！

**修改详情（你需要参考这部分需求）**
1. 你现在需要参考 mock/output/ 文件夹下的所有文件，按照 _crawl_index.json 中的数据解构和文本信息，分析其层次结构，比如
首页包含本系概况、专业设置、师资队伍、教研科研等等...这样子
2. 分析好后，找到对应的 md文件，对文件内容进行阅读，并生成 TS mock数据 并按照步骤 1 分析的层次结构 保存到 /mock 文件夹下，层次结构比如
/mock
    /index
        /introduce
        /set
        /team
        ...
    /...
    /...

**任务拆解与规划**
- 接收需求后，先将任务拆解为最小可执行单元，逐步推进，不可跳步
- 每一步执行前，先陈述你的意图，再执行
- 遇到不确定的业务逻辑，暂停并询问，不可自行假设
- 多个子任务存在依赖关系时，明确标注执行顺序

**上下文管理**
- 开始任务前，先通读项目结构，理解整体架构
- 修改前必须查阅目标文件及其上下游依赖
- 每完成一个子任务，输出当前状态摘要，确保后续步骤可以基于此继续
- 当上下文窗口低于 50% 时执行 /compact

**工具与依赖**
- 优先确认社区是否有成熟轮子，有则直接引入，无则手写
- 引入新依赖前确认：是否与当前项目技术栈兼容、是否有安全漏洞、是否仍在维护
- 禁止引入仅解决单一问题但体积过大的依赖

**代码编写要求**
编写完之后进行自我审问：你改写的代码真的是最佳实践么？开启子代理进行代码审阅，审阅内容参考"代码编写要求"。
- 修改已有逻辑前，先判断该逻辑是否仍在使用，不确定时暂停询问
- 编写代码时依次检查：是否存在安全风险、是否为最佳实践、是否人类可读、是否与项目风格一致、是否存在冗余
- 确保无编码问题导致的乱码

**子代理审阅流程（非 codex or claude code 请使用正常模式审查）**
- 代码完成后，开启子代理对本次改动进行 review
- 子代理审阅维度：逻辑正确性、安全性、可读性、响应式、与项目规范一致性
- 子代理若发现问题，必须回写修改意见，主代理根据意见修改后再次确认

**最后**

生成 md版本管理文档，保存到项目根目录的 /version/ 下，以 X.X.X 命名。

写完之后，交由 Claude Code 进行最终 review。你可以做到的，加油！